# Goldberg 300 – план доработок

## State machine
- [x] Задать `phaseTotalSec` для каждой фазы по программе (preheat, prevacuum, heat_up, sterilization, drying, depressurize, cooling).
- [x] Чёткие условия переходов: по времени, температуре и давлению; счётчик предвакуумов.
- [x] Корректное завершение цикла с записью `CycleSummary` (успешно/ошибка/стоп) и выбросом ошибок на порогах.

## Логи в UI
- [x] В таблице циклов выводить дату/время, программу, статус, макс. T/P, длительность.
- [x] Добавить сортировку/фильтр по статусу (успех/ошибка).
- [x] Экспорт: JSON (уже есть) + CSV для циклов/ошибок.

## Remote режим
- [x] Переключатель `mode: local | remote` в UI.
- [x] В remote: подключение к `ws://<gateway>` по протоколу `state`/`command`, команды уходят через WebSocket.
- [x] Fallback на local simulation при обрыве (авто-реинициализация).

## Физика и ошибки
- [ ] Подкрутить коэффициенты нагрева/вакуумов, чтобы выход на 134°C/0.2–0.3 МПа занимал реалистичное время.
- [ ] Добавить сценарные ошибки: timeout сушки, door-open mid-cycle, unstable sensor.

## Инфраструктура
- [ ] Запуск lint/test в CI.
- [ ] Проверка Docker-сборки (UI + gateway) как отдельного шага.
